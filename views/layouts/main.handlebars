<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TechTalk Blog</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"
    integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="wrapper">
    <header>
      <h4>
        <a href="/">TechTalk Blog</a>
      </h4>
      <nav>
        {{#if logged_in}}
        <a href="/dashboard"><span class="glyphicon glyphicon-user"></span> {{ username }}'s Dashboard</a>
        <button id="logout" class="btn-no-style">Log Out</button>
        {{else}}
        <a href="/login">Log In</a>  or  <a href="/signup">Sign In</a> 
        {{/if}}
      </nav>
    </header>
    <main>
      {{{ body }}}
    </main>
    <footer>
      Pied Piper Development Team - Aiming to Replace Twitter in 2023?
    </footer>
  </div>

</body>

</html>

{{#if logged_in}}
<script src="/js/logout.js"></script>
{{/if}}